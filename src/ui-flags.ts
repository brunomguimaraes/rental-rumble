// UI presentation flags — single source of truth for cosmetic toggles that
// affect what players see but not how the game plays.

/**
 * Show the Physical/Energy build badge on creature cards and in the moves modal.
 * Hidden for now so players discover their Pokémon's lean themselves. The build
 * still drives stats and movesets regardless — this only controls the label.
 */
export const SHOW_BUILD_TAG = false;
