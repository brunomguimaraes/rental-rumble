/// <reference types="vite/client" />

// Injected by Vite's `define` from package.json at build time — the running
// app version, e.g. "0.1.0". See vite.config.ts.
declare const __APP_VERSION__: string;
